Benchmarks Module
=================

Dataset loaders for standard forecasting benchmarks.

Provides loaders for FRED economic data, M5 competition data,
Monash forecasting archive, and GluonTS datasets.

.. note::

   Each loader requires optional dependencies. Install with:

   .. code-block:: bash

      pip install temporalcv[fred]      # FRED economic data
      pip install temporalcv[monash]    # Monash archive
      pip install temporalcv[gluonts]   # GluonTS datasets
      pip install temporalcv[benchmarks]  # All benchmark loaders

Module Contents
---------------

.. automodule:: temporalcv.benchmarks
   :members:
   :undoc-members:
   :show-inheritance:
